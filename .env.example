# URL base da API (sem trailing slash).
# Em dev local: não é necessário - o proxy Vite usa /api.
# Em produção (ex: Cloudflare Pages): defina a URL do Worker, ex:
# VITE_API_URL=https://app-billing.SEU-SUBDOMINIO.workers.dev/api
